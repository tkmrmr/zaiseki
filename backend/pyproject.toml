[tool.ruff]
extend-exclude = [
    "cgi-bin/zaiseki/api/vendor",
]

[tool.ty.src]
include = ["cgi-bin/zaiseki/api"]
exclude = [
    "cgi-bin/zaiseki/api/vendor",
]

[tool.ty.environment]
root = ["cgi-bin/zaiseki/api"]
python-version = "3.10"
python = "./.env"
